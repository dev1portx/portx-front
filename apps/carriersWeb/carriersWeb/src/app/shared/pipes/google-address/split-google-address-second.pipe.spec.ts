import { SplitGoogleAddressSecondPipe } from './split-google-address-second.pipe';

describe('SplitGoogleAddressSecondPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitGoogleAddressSecondPipe();
    expect(pipe).toBeTruthy();
  });
});
