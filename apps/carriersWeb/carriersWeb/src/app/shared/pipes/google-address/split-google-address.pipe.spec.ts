import { SplitGoogleAddressPipe } from './split-google-address.pipe';

describe('SplitGoogleAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitGoogleAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
