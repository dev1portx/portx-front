import { FadeinoutDirective } from './fadeinout.directive';

describe('FadeinoutDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeinoutDirective();
    expect(directive).toBeTruthy();
  });
});
